= f.simple_fields_for :data, OpenStruct.new(f.object.data) do |ff|
  .input-group-addon None
  = ff.input :message, label: false, placeholder: f.object.class.human_attribute_name(:message), error: false
