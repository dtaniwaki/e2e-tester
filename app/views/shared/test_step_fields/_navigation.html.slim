= f.simple_fields_for :data, OpenStruct.new(f.object.data) do |ff|
  .input-group-addon Navigate to
  = ff.input :url, label: false, placeholder: f.object.class.human_attribute_name(:url), error: false
