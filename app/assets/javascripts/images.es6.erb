$.extend(true, $, {
  images: {
    loading: "<%= image_path('loading.gif') %>",
    failed: "<%= image_path('failed.png') %>",
    zeroclipboard: "<%= image_path('zeroclipboard/dist/ZeroClipboard.swf') %>"
  }
})
